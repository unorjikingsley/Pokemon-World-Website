const getCommentsCount = (comments) => comments.length;
const getLaunchesCount = (launches) => launches.length;
const getReservationCount = (reservations) => reservations.length;

export { getLaunchesCount, getCommentsCount, getReservationCount as default };
